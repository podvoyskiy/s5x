name: Build Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Rust toolchain
      uses: actions-rust-lang/setup-rust-toolchain@v1
      with:
        toolchain: 1.95.0
        target: x86_64-unknown-linux-musl
    
    - name: Build s5x binary
      run: cargo build --release -p s5x --target x86_64-unknown-linux-musl

    - name: Strip Linux binary
      run: strip target/x86_64-unknown-linux-musl/release/s5x

    - name: Upload Linux binary
      uses: softprops/action-gh-release@v1
      with:
        files: target/x86_64-unknown-linux-musl/release/s5x